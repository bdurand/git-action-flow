name: Jira Transitions

on:
  pull_request:
    types:
      - labeled
      - unlabeled

jobs:
  build:
    if: contains(github.event.label.name, 'QA Ready')
    runs-on: ubuntu-latest
    name: Transition Jira Task From Labels
    env:
      JIRA_API_URL: ${{ secrets.JIRA_BASE_URL }}/rest/api/3
      JIRA_USER_EMAIL: ${{ secrets.JIRA_USER_EMAIL }}
      JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
    steps:
      - name: Find Jira Issue
        id: find_jira_issue
        env:
          search_string: ${{ github.event.pull_request.head.ref }} ${{ github.event.pull_request.title }} ${{ github.event.pull_request.body }}
          patterns: \b(ENG)-([0-9]+)
        run: |
          issue=$(echo $search_string | grep -Eoi -e $patterns)
          if [ "$issue" != "" ]; then
            echo "::set-output name=issue::${issue}"
          fi
      - name: Get Issue Status
        id: jira_issue_status
        if: steps.find_jira_issue.outputs.issue
        run: |
          url="${JIRA_API_URL}/issue/${{ steps.find_jira_issue.outputs.issue }}?fields=status"
          status_response=$(curl -s -u ${JIRA_USER_EMAIL}:${JIRA_API_TOKEN} -X GET -H "Accept: application/json" $url)
          status_name=$(echo $status_response | jq -r .fields.status.name)
          echo "::set-output name=status_name::${status_name}"
      - name: Get New Status
        id: get_new_status
        if: steps.find_jira_issue.outputs.issue
        run: |
          issue_key="${{ steps.find_jira_issue.outputs.issue }}"
          issue_status="${{ steps.jira_issue_status.outputs.status_name }}"
          action="${{ github.event.action }}"
          new_status=""
          if [ "$action" == "labeled" ]; then
            new_status="QA Ready"
          elif [ "$action" == "unlabeled" ]; then
            new_status="In Progress"
          fi
          if [ "$new_status" != "" ] && [ "$new_status" != "$issue_status" ]; then
            echo "::set-output name=status_name::${new_status}"
          fi
      - name: Transition Issue
        if: steps.get_new_status.outputs.status_name
        run: |
          issue_key="${{ steps.find_jira_issue.outputs.issue }}"
          new_status="${{ steps.get_new_status.outputs.status_name }}"
          status_response=$(curl -s -u ${JIRA_USER_EMAIL}:${JIRA_API_TOKEN} -X GET -H "Accept: application/json" ${JIRA_API_URL}/status)
          status_id=$(echo $status_response | jq ".[] | select(.name==\"${new_status}\") | .id")
          transition_url="${JIRA_API_URL}/issue/${{ steps.find_jira_issue.outputs.issue }}/transitions"
          payload="{\"transition\": {\"id\": \"${status_id}\"}}"
          echo $transition_url
          echo $payload
          curl -s -u ${JIRA_USER_EMAIL}:${JIRA_API_TOKEN} -X POST -H "Accept: application/json" -H "Content-Type: application/json" --data $payload $url
     # - name: Transition Issue To QA Ready
     #    if: steps.find_jira_issue.outputs.issue && github.event.action == 'labeled'
     #    uses: atlassian/gajira-transition@v2.0.2
     #    with:
     #      issue: ${{ steps.find_jira_issue.outputs.issue }}
     #      transition: "QA Ready"
     #  - name: Transition Issue To In Progress
     #    if: steps.find_jira_issue.outputs.issue && github.event.action == 'unlabeled'
     #    uses: atlassian/gajira-transition@v2.0.2
     #    with:
     #      issue: ${{ steps.find_jira_issue.outputs.issue }}
     #      transition: "In Progress"
