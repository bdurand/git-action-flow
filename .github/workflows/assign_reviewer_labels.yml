name: Assign Reviewer Labels
on:
  pull_request:
    types:
      - review_requested

jobs:
  apply_labels:
    name: Apply Labels
    runs-on: ubuntu-latest
    steps:
      - name: Assign Labels
        run: |
          cat $GITHUB_EVENT_PATH
